# Environment Configuration for The Penthouse

# Server
PORT=3000
NODE_ENV=development

# Security - CHANGE THIS IN PRODUCTION!
# Generate: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS - Restrict to your mobile app's origin in production
# Use * for development, or your domain for production
CORS_ORIGIN=*

# Domain (for production CORS/cookies)
DOMAIN=localhost

# Persistent app data directory on host (recommended in production)
# Example: /mnt/Storage_Pool/penthouse/data
# PENTHOUSE_DATA_PATH=./data

# Upload limits
# Default server icon max is 25MB (26214400)
# SERVER_ICON_MAX_BYTES=26214400

# GIF APIs (optional - get keys from giphy.com and klipy.co)
GIPHY_API_KEY=your-giphy-api-key
KLIPY_API_KEY=your-klipy-api-key

# Email (optional - for password recovery)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=The Penthouse <your-email@gmail.com>
