# Handoff: The Penthouse Editorial Landing Page

## Context
The user has selected **Concept 01 (Floating Orb)** from the `poc-landing-pages-v5` series as the winning aesthetic for the new "The Penthouse" landing page.

## Artifacts
- **Chosen Design:** `apps/web/src/routes/welcome/REFERENCE_DESIGN.html` (Copy also at root: `landing-page-reference.html`)
- **Asset Requirements:**
  - Erode font (CDNFonts)
  - Ubuntu & JetBrains Mono (Google Fonts)
  - SVG icons (inline in the HTML)
  - CSS-only grain and atmospheric orbs (no external images)

## Task for Claude
1. **Integrate into SvelteKit:** 
   - Migrate the selected HTML/CSS/JS from `concept-01.html` into the SvelteKit application (`apps/web`).
   - Determine if this should replace the current root route (`/`) or be a separate splash page.
   - Note: The current root route (`+page.svelte`) is the Chat List, but the project might benefit from a dedicated public-facing landing page at `/` for unauthenticated users.
2. **Refine & Finalize:**
   - Ensure the "Enter the app" liquid transition correctly navigates to the `/auth` route.
   - Verify the PWA/APK installation section works with the backend `https://api.penthouse.blog/api/v1/app-distribution` endpoint.
   - Polish the "What this is" section with the provided editorial copy.
3. **Deploy:**
   - Ensure the styles are scoped correctly (Svelte components) or managed via a global stylesheet.
   - Confirm responsive behavior on mobile (editorial layouts can be tricky).

## Selected Aesthetic Constraints
- **Dark Mode Default:** `#0D0D14`
- **Accent Color:** `#7777C2` (Bright Periwinkle)
- **Atmosphere:** Floating blurred orbs, grain texture (4% opacity).
- **Logo Hierarchy:** Stacked Erode font, 2x size increase for "PENT" and "HOUSE".

---
*Generated by Gemini CLI on 2026-04-15*
